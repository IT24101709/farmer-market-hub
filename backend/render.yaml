services:
  - type: web
    name: farmers-market-hub-backend
    env: node
    plan: free
    buildCommand: npm install && npm install mongodb-memory-server
    startCommand: node run.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
